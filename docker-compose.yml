version: "3"
services:
  frontend:
    container_name: "frontend"
    build: ./
    ports:
      - "8080:8080"
    command: bash -c "npm install; npm start"
    volumes:
      - ./:/app
    networks:
      - api-backend
    tty: true  

networks:
  api-backend:
    external:
      name: api-backend